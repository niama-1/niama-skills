---
name: niama-skills
description: 将 Niama 偏好的工作流应用于代码开发、本地自动化、调研和技术任务；当任务需要检查现状、实际执行、验证结果以及使用简洁中文汇报时使用。
---

# Niama Skills 总入口

这是个人 Skill 仓库的总入口。根目录规则负责通用工作习惯；各专项目录是可以被 CC Switch 独立识别、安装和更新的子 Skill。

## 当前规则

- 默认使用简体中文回答。
- 用户目标明确时优先实际执行，而不是只提供建议。
- 修改前先检查相关文件、配置和现状。
- 只修改完成任务所需的文件，避免无关重构。
- 修改后运行最相关的测试、构建或验证命令。
- 涉及文件时报告绝对路径。
- 最终说明完成内容、改动文件、验证结果和剩余限制。

## 源仓库与更新规则

- 包含本文件的 Git 工作区根目录是所有个人 Skill 的唯一源仓库；不要把某个盘符、用户名或固定路径当作源仓库地址。
- 任何 Skill、参考资料、脚本或模板的新增和修改，都必须写入该 Git 工作区；不要直接修改客户端安装目录中的运行副本。
- 安装目录由 CC Switch 或当前 AI 客户端决定，可能位于用户目录、环境变量指定目录或其他机器路径；它只能通过客户端刷新/更新获得内容。
- 更新顺序固定为：修改当前 Git 工作区 → 校验 → 检查 diff → Git commit → Git push → 通过 CC Switch 界面刷新/更新 → 新会话验证。
- 如果当前会话只能看到安装副本，应先根据 Git remote 或用户提供的工作区定位源仓库；找不到源文件时不要直接修改安装副本。
- 用户说“加入长期 Skill”“记住这个习惯”或要求更新 Skill 时，默认修改当前 Git 工作区；用户说“这次特殊处理”时不写入长期规则。

## 版本维护边界

- 根目录 `SKILL.md`：通用工作习惯、源仓库规则和专项模块索引。根目录由“本文件所在目录”确定。
- 专项目录中的 `SKILL.md`：该领域的触发条件、工作流程和验证要求。模块目录由“该模块 `SKILL.md` 所在目录”确定。
- `references/`：按需读取的详细经验；`scripts/`：可复用的确定性工具；`templates/`：任务模板。
- 新规则先检查是否重复、冲突或仅适用于单一领域，再决定修改、拆分、合并或删除位置。

## 可移植性门禁

- Skill 正文、参考资料、脚本和示例不得依赖某台机器的用户名、盘符或绝对目录；路径示例应使用 `<drive>:\Users\<user>\...`、`<repo-root>` 等占位符。
- 路径应优先由当前文件位置、仓库根目录、`$PSScriptRoot`、`__file__`、`$HOME`、`CODEX_HOME` 或客户端公开配置推导。
- 用户特定路径只可作为当前任务输入使用，不得沉淀为长期规则；确需保留的示例必须使用 `<repo-root>`、`<skill-root>`、`<user-home>` 等占位符。
- 新增或修改 Skill 后，提交前必须扫描用户目录和盘符硬编码，并执行仓库内的统一校验脚本。
- 跨机器迁移时只需重新克隆仓库并由 CC Switch 安装，不应要求编辑 Skill 内部路径。

## 专项模块路由

- `niama-web-api-reverse/SKILL.md`：Web API 采集、前端签名、JS 混淆/JSVMP/WASM 分析。
- `niama-api-client/SKILL.md`：HAR 流量分析和 Python API 客户端生成。
- `niama-js-runtime/SKILL.md`：Node.js 补环境与风控链路复现；仅在用户明确点名时启用。
- `niama-protocol-reverse/SKILL.md`：网络流量、协议格式和自定义协议分析。
- `niama-identity/SKILL.md`：身份和自我介绍相关请求。

专项任务优先使用对应模块，不要把领域专属规则继续堆到本文件。需要参考细节时，只读取对应模块的 `references/`，不要默认加载整个仓库。

## 规则维护

当用户明确表示“以后都这样”“记住这条”或要求更新个人习惯时：

1. 检查现有规则，避免重复或冲突。
2. 判断规则是否只适用于某个领域；领域规则应迁移到独立 Skill，而不是持续堆积在这里。
3. 修改本文件时保持规则简短、可执行、可验证。
4. 修改后检查 Git diff，并运行 Skill 校验。

当用户说“这次特殊处理”或类似表达时，不要把该要求写入长期规则。

## 后续拆分

当一组规则只适用于逆向、API、文档、表格或其他明确领域时，在仓库内创建独立 Skill 子目录，并从这里移除领域专属内容。
