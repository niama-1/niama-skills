# 补环境进展清单（已降级）

`补环境进展清单.md` 如果在旧项目中存在，只能当人工摘要。AI 不创建、不维护、不续写，也不能把它当作状态源、续跑依据或已补环境事实来源。

阶段 4 以后，AI 的状态只从以下材料判断：

1. `target.lock`：当前允许处理的目标集合；
2. `code.js` / `runtime/*.code.js`：已经实际补了什么；
3. `proxylog.txt`：最新一轮本地缺口；
4. 当前入口运行结果 / 报错；
5. 匹配当前目标的 `ruyitrace/` 证据。

如果人工仍想保留摘要，只建议记录下面两项；该摘要不能替代代码、日志和 trace。

## 当前卡点

- 当前阶段:
- 卡在哪一步:
- 当前现象:
- 下一步处理:

## 已补环境

| 环境区域 | 已补内容 | 状态 |
|---|---|---|
| window |  | 待确认 |
| document |  | 待确认 |
| location |  | 待确认 |
| navigator |  | 待确认 |
| screen |  | 待确认 |
| storage |  | 待确认 |
| event/worker/async |  | 待确认 |
| fingerprint |  | 待确认 |
| window.performance/history |  | 待确认 |

状态只使用以下三种：

- `已补待验证`
- `已验证`
- `待确认`

没有补过的环境区域可以删除；不相关的区域不要为了“完整”填写空壳。AI 不因该文件缺失、过期或过短而回退盘点。
